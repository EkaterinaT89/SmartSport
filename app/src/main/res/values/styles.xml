<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Widget.AppTheme.ChooseButtonStyle" parent="Widget.MaterialComponents.CompoundButton.CheckBox">
        <item name="backgroundTint">@drawable/button_select</item>

    </style>

    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/grey</item>
        <item name="colorControlActivated">@color/blue_primary</item>
    </style>

    <style name="MineCustomTabText" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">10sp</item>
    </style>

    <!-- стиль для заднего фона -->
    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:titleTextStyle">@style/MyTextAppearance</item>
    </style>
    <!-- стиль для текста -->
    <style name="MyTextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>